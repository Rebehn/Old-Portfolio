'use strict';

$(document).ready(function () {
  $('#particles').particleground({
    dotColor: 'white',
    lineColor: 'white',
    lineWidth: 0.5,
    proximity: 100,
    maxSpeedX: 0.2,
    maxSpeedY: 0.2
  });
});